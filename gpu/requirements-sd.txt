# Memory Pager — GPU 서버, Stable Diffusion 전용 venv
#
#   python3 -m venv ~/envs/sd && source ~/envs/sd/bin/activate && pip install -r gpu/requirements-sd.txt
#
# vLLM 환경(gpu/requirements-vllm.txt)과 torch 핀이 충돌하므로 venv 를 반드시 나눈다.
# 호스트에 CUDA 툴킷을 깔지 마라. torch 가 CUDA 런타임을 wheel 에 번들한다.

torch==2.9.0        # requires Python>=3.10
diffusers==0.39.0   # 2026-07-03 최신. SD 1.5 파이프라인
fastapi==0.139.0
uvicorn[standard]==0.51.0
pydantic-settings==2.14.2
pillow==12.3.0

# SD 1.5 P1 실행 경로. transformers 5.x 전환 리스크를 피하려고 검증된 4.x 끝 버전을 고정한다.
transformers==4.57.6
accelerate==1.12.0

# peft·bitsandbytes는 LoRA(PT-5/PT-6b, P2)에서만 필요하므로 P1 환경에 섞지 않는다.

# VRAM (docs/STACK.md 2절, 검증됨. 카드는 실측 24GB):
#   SD 1.5 추론 (512px, fp16, 1장)              4~6GB   -> 상주 LLM 켠 채로 가능
#   SD 1.5 LoRA 학습 (512px, batch1, 8bit Adam) 6~8GB   -> 상주 LLM 켠 채로 가능 (24GB 에서 헤드룸 8~10GB)
#
# SDXL 은 쓰지 않는다. 24GB 에서 상주 LLM 과 겨우 공존은 하나(합 21~23GB) 헤드룸이 없어 OOM 위험이 있다.
