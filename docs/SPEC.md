# Memory Pager — 소프트웨어 스펙 문서 (SDD)

| 항목 | 내용 |
|---|---|
| 프로젝트명 | Memory Pager (가칭) |
| 팀 | 26s-w2-c1-04 — 이종혁(DGIST), 안종화(KAIST) |
| 문서 버전 | v0.3 (2026-07-10) |
| 대상 플랫폼 | **Android (갤럭시)** 단일 |
| 과제 옵션 | **주: 실시간 인터랙션** / 부: LLM Wrapper |
| 인프라 예산 | **0원** — 외부 유료 API 미사용 |
| 서버 ① 앱 VM | **Ubuntu 24.04** / vCPU 4 / **RAM 4GB** / DISK 100GB / 개방 포트 22·80·443 |
| 서버 ② GPU | **Ubuntu 20.04** / vCPU 40 / RAM 50GB / DISK 100GB / **RTX 3090 (VRAM 20GB) ×1** |
| 성격 | 포트폴리오용 사이드 프로젝트. **보안 하드닝은 범위 밖**(1.2절) |
| 원본 | `기능정의서 2조.xlsx` (**우선**), `몰입캠프 2주차 Team 4.docx` (보조) |
| 관련 문서 | [API.md](API.md) · [ERD.md](ERD.md) · [STACK.md](STACK.md) · [BACKLOG.md](BACKLOG.md) |

> **문서 우선순위 원칙.** 두 원본이 충돌하면 **기능정의서를 따른다.** 기획 문서에만 있고 기능정의서에 없는 기능은 구현 범위에서 빼고 [BACKLOG.md](BACKLOG.md)에 기능 후보로 분리했다.

---

## 1. 개요

커플이 사진과 손글씨 낙서를 주고받으며 일상을 기록하는 **폐쇄형 소셜 앱**. "삐삐(pager)"의 아날로그 감성을 컨셉으로, 상대방의 홈 화면 위젯에 즉시 노출되는 짧은 메시지 경험을 제공한다.

앱의 중심에는 **AI 반려 펫**이 있다. 펫은 스스로 하루를 산다 — 밥을 먹고, 잠을 자고, 산책을 한다. 쓰다듬으면 지금 뭘 하는 중인지 알려주고, 하루가 끝나면 그날을 **두 사람의 그림체로 그린 일기 한 장**으로 남긴다.

### 1.1 목표

1. 사진 + 낙서를 몇 초 안에 전달하고, 홈 화면 위젯으로 즉시 확인시킨다.
2. 찌르기·사라지기 모드로 **실시간 양방향 상호작용**을 만든다. — *과제 주 옵션*
3. 펫이 하루를 살고, 말을 걸고, 그림 일기를 남긴다. — *과제 부 옵션(LLM)*
4. 누적 활동을 펫 성장과 월간 레포트로 환원해 재방문 동기를 만든다.

### 1.2 비목표

- iOS 지원 (갤럭시 단일 타깃)
- 공개 타임라인, 팔로우, 댓글 등 오픈 소셜 그래프
- 1:1 채팅 / 음성 / 영상 통화
- 현금 결제 (스토어 아이템은 코인으로만 구매)
- **유료 외부 API 사용** (예산 0원)
- **보안 하드닝** — 인증 강화, rate limiting, 시크릿 관리, 입력 검증 강화 등. 포트폴리오용 사이드 프로젝트이므로 기능 구현에 집중한다. 실서비스로 전환한다면 6.6절의 인증부터 다시 봐야 한다.

### 1.3 용어

| 용어 | 정의 |
|---|---|
| **그룹 (Group)** | 폐쇄형 공간의 최소 단위. 초대 코드로 가입하며 **정원 2명(커플)**. |
| **낙서 (Doodle)** | 전송 단위 1건. 사진·손그림·텍스트의 조합. |
| **일반 모드** | 다음 낙서를 보낼 때까지 유지. 레포트 집계에 반영된다. |
| **사라지기 모드** | 수신자가 확인한 시점부터 5초 뒤 삭제. 레포트에 반영되지 않는다. |
| **찌르기 (Poke)** | 내용 없이 알림만 보내는 가벼운 안부 인터랙션. |
| **펫 (Pet)** | 그룹당 1마리. 그룹 활동으로 성장한다. |
| **펫 활동 (Pet Activity)** | 펫이 지금 하고 있는 일(밥 먹기·잠·산책 등). LLM이 생성하며, 쓰다듬으면 이 활동을 말해준다. |
| **그림 일기 (Pet Diary)** | 하루치 펫 활동을 그림 한 장 + 캡션으로 남긴 기록. 모이면 일기장이 된다. |
| **그림체 모델 (Style Model)** | 일기 그림을 그릴 화풍. 처음엔 **기본 프리셋**, 낙서가 쌓이면 **사용자 그림체 LoRA**로 교체된다. |

---

## 2. 정보 구조 (IA) · 진입 경로

기능정의서 22–24행이 앱의 진입 경로를 규정한다.

| 진입점 | 도착 화면 | 근거 |
|---|---|---|
| 앱 실행 | **펫 키우기 (홈)** | 22행 |
| 홈 화면 위젯 탭 | **낙서 사진첩** | 23행 |
| 월간 알람 탭 | **월간 레포트 뷰어** | 24행 Description은 "소통"이나, 레포트 알림이 레포트로 가는 것이 옳다는 팀 결정. [BACKLOG.md](BACKLOG.md) B절에 예외로 기록 |

펫 키우기 화면이 앱의 첫 화면이자 중심이다. 낙서를 주고받는 행위가 펫을 키우고, 펫이 그 결과를 일기로 되돌려주는 순환 구조다.

---

## 3. 사용자 시나리오

1. A가 앱을 연다. 첫 화면에 펫이 낮잠을 자고 있다. 쓰다듬으니 *"방금 밥 먹고 졸려…"* 라고 말한다.
2. A가 사진을 찍고 그 위에 손글씨로 낙서한 뒤 *일반 모드*로 전송한다.
3. B의 홈 화면 위젯이 즉시 갱신되고 푸시 알림이 도착한다. 위젯을 탭하면 사진첩으로 들어간다.
4. B가 답장 낙서를 그려 보낸다. 펫의 경험치가 오른다.
5. 하루가 끝나면 펫이 그날의 하루를 그림 일기 한 장으로 남긴다. 처음엔 기본 그림체지만, 두 사람의 손그림이 충분히 쌓이면 **그날 이후의 일기부터 두 사람의 그림체로 그려진다.**
6. A가 *찌르기*를 보낸다. B의 화면에 즉시 알림이 뜬다.
7. 월말, 월간 레포트 알림이 도착한다. 펫 성장 폭, 이번 달 최고의 낙서, 낙서 유형 분포를 확인한다.

---

## 4. 기능 명세

`기능정의서 2조.xlsx`의 36개 행을 매핑했다. **우선순위**는 7일 일정 기준.

- **P0** — 없으면 서비스가 성립하지 않음. Day 1–4.
- **P1** — 과제 옵션(실시간·LLM)을 입증하는 핵심. Day 4–6.
- **P2** — 여유가 있을 때. 잘라낼 수 있어야 함.

### 4.1 온보딩 · 그룹

| ID | 기능정의서 | 설명 | 우선순위 |
|---|---|---|---|
| ON-1 | 내 이름 작성하기 | 최초 실행 시 표시 이름 설정 | P0 |
| ON-2 | 그룹 만들기 | 생성 시 초대 코드 자동 발급 | P0 |
| ON-3 | 그룹 가입 | 초대 코드 입력해 가입 (정원 2명) | P0 |
| ON-4 | 별명 지어주기 | 상대가 들어오면 그룹 내 별명 부여 | P1 |
| GR-1 | 그룹 이름 변경 | | P1 |
| GR-2 | 배경 색상 변경 | 그룹 테마 색 | P2 |
| ST-1 | 설정 화면 | ON-1·ON-2·ON-4 사후 재편집 (34–36행) | P2 |

### 4.2 소통 — 보내기 ★실시간

| ID | 기능정의서 | 설명 | 우선순위 |
|---|---|---|---|
| SD-1 | 펜 선택 | 펜 종류 선택 | P0 |
| SD-2 | 색깔 선택 | 캔버스 색상 선택 | P0 |
| SD-3 | 사진 찍기 | 카메라 촬영 | P0 |
| SD-4 | 사진 선택 | 갤러리에서 선택 | P0 |
| SD-5 | 일반 모드 | 다음 전송 전까지 유지, 레포트 반영 | P0 |
| SD-6 | 사라지기 모드 | 수신자 확인 후 5초 뒤 삭제, 레포트 미반영 | **P1** |
| SD-7 | 찌르기 | 상대에게 알림만 전송 | **P1** |
| SD-8 | 낙서 수신 푸시 | 낙서 도착 시 FCM 푸시 (팀 결정으로 스코프 편입) | **P1** |

### 4.3 소통 — 확인하기

| ID | 기능정의서 | 설명 | 우선순위 |
|---|---|---|---|
| RV-1 | 답장 보내기 | 수신 낙서에 펜·색 선택해 답장 | P0 |
| RV-2 | 낙서 사진첩 — 날짜 정렬 | | P0 |
| RV-3 | 낙서 유형 표시 | 사진 위주 / 텍스트 위주 / 그림 위주 | P1 |
| RV-4 | 앱 뷰어 | 이전·다음 낙서 이동 | P0 |
| RV-5 | 위젯 뷰어 | 홈 화면 위젯 → 사진첩 진입 | **P1** |

### 4.4 펫 키우기 ★LLM

| ID | 기능정의서 | 설명 | 우선순위 |
|---|---|---|---|
| PT-1 | 펫 뷰어 · 쓰다듬기 | **지금 뭘 하는 중인지 말해준다** → LLM이 활동+대사 생성 | **P1** |
| PT-6a | 펫의 그림 일기 (기본 그림체) | 하루치 활동을 **기본 프리셋 그림체**로 1장 생성 + 캡션 | **P1** |
| PT-2 | 펫 커스텀 | 옷 / 모자 / 기타 | P2 |
| PT-3 | 펫 집 꾸미기 | 가구·배경·소품 배치 | P2 |
| PT-4 | 펫 스토어 | 코인으로 아이템 구매 | P2 |
| PT-5 | 그림체 학습 | 손그림 낙서 N장 이상 → SD LoRA 파인튜닝 | P2 |
| PT-6b | 일기장 그림체 전환 | 학습 완료 후의 일기부터 **사용자 그림체**로 생성 | P2 |

> **PT-6a와 PT-6b를 나눈 이유.** 기능정의서 33행은 "펫의 그림 일기장"이라고만 적혀 있고 그림체 학습(31행)과 별개 행이다. 일기장은 첫날부터 동작해야 하므로 **기본 그림체로 P1에 넣고**, 그림체 학습과 전환은 P2로 뺐다. LoRA가 무너져도 일기장은 살아남는다.

### 4.5 다른 그룹 펫 구경하기

| ID | 기능정의서 | 설명 | 우선순위 |
|---|---|---|---|
| EX-1 | 좋아요 | 다른 그룹 펫에 좋아요 | P2 |
| EX-2 | 찾아가기 | 코드 입력해 특정 펫 구경 | P2 |
| EX-3 | 옆으로 넘기기 | 다음 펫 탐색 | P2 |

### 4.6 월간 레포트

| ID | 기능정의서 | 설명 | 우선순위 |
|---|---|---|---|
| MR-1 | 월간 알람 | 월말 푸시 발송 → 월간 레포트 뷰어 진입 | P1 |
| MR-2 | 펫 성장 표시 | 레벨 변화 | P1 |
| MR-3 | 이번 달 최고의 낙서 | **규칙 기반 선정** (6.3절) | P1 |
| MR-4 | 낙서 유형 분포 | | P1 |

---

## 5. 시스템 아키텍처

```
┌──────────────────────────┐
│  Flutter App (Android)   │
│  ├─ Canvas (낙서)        │  + 홈 화면 위젯
│  ├─ Camera / Gallery     │    (Android AppWidget — 네이티브 Kotlin)
│  └─ Pet Renderer         │
└────────┬─────────────────┘
         │ HTTPS (REST)        · 인증, 그룹, 사진첩, 레포트, 펫
         │ WSS (Socket.IO)     · 찌르기, 신규 낙서, 사라지기 만료
         ▼
╔═══════════════════════════════╗       ╔══════════════════════════════════╗
║  ① 앱 VM                      ║       ║  ② GPU 서버                      ║
║  Ubuntu 24.04                 ║       ║  Ubuntu 20.04                    ║
║  4 vCPU · 4GB RAM · 100GB     ║       ║  40 vCPU · 50GB RAM · 100GB      ║
║  개방 포트 22 · 80 · 443      ║       ║  RTX 3090 (VRAM 20GB) ×1         ║
╟───────────────────────────────╢       ╟──────────────────────────────────╢
║  FastAPI (:8000)              ║       ║  LLM 서빙 (상주, :8100)          ║
║  ├─ REST Router               ║──────▶║   펫 활동·대사·일기 캡션         ║
║  ├─ Socket.IO Server          ║  HTTP ║                                  ║
║  ├─ Media Server (/media/*)   ║◀──────║  SD 1.5 추론 (자정 배치, :8200)  ║
║  └─ Scheduler                 ║  PNG  ║   일기 그림 생성                 ║
║                               ║  응답  ║                                  ║
║  MySQL 8                      ║       ║  SD LoRA 학습 (새벽 배치, P2)    ║
║  파일시스템 (미디어)          ║       ║  파일시스템 (모델 가중치)        ║
╚═══════════════════════════════╝       ╚══════════════════════════════════╝
         ▲
         │ WSS + HTTPS (Cloudflare Tunnel → 443)
         │
   Flutter App (Android)
```

> **두 대다.** 앱 VM이 상태(DB·미디어)를 전부 쥐고, GPU 서버는 **상태 없는 추론 워커**다. 앱 VM이 GPU 서버를 동기 HTTP로 부르고 PNG 바이트를 받아 저장한다. 이렇게 하면 GPU 서버가 죽어도 앱은 계속 돌고, 펫 대사는 `pet_activities` 캐시로 버틴다. 내부 API는 [API.md](API.md) 11절.
>
> **앱 VM의 RAM이 4GB뿐이다.** MySQL 8의 `innodb_buffer_pool_size` 기본값(128MB)을 크게 올리지 마라. 이미지 리사이즈 같은 무거운 작업도 앱 VM에서 하지 않는다.
>
> **두 서버가 서로 보이는지는 아직 확인되지 않았다.** 7절 ⑧ 참조.

### 5.1 배포 제약 (README 환경표 기준)

KCLOUD VM + Cloudflare Tunnel 환경에서:

- ✅ **가능** — HTTP/REST, WebSocket, Socket.IO, SSE
- ❌ **불가** — 순수 TCP 소켓, UDP, WebRTC 미디어/DataChannel, QUIC/WebTransport
- ⚠️ gRPC는 보장 어려움

따라서 실시간 계층은 **Socket.IO(WebSocket transport)** 로 확정한다. FastAPI는 앱 VM의 `8000`에 바인딩하고 Tunnel로 443에 노출한다. 앱 VM의 개방 포트는 22·80·443뿐이므로 `8000`을 직접 열지 않는다.

**GPU 서버는 인터넷에 노출하지 않는다.** 앱 VM만 내부 네트워크로 호출한다.

### 5.2 기술 스택

| 계층 | 기술 | 선택 근거 |
|---|---|---|
| OS | 앱 VM은 **Ubuntu 24.04**(Python 3.12), GPU 서버는 **Ubuntu 20.04**(Python 3.8) | 7절 ③ — Python 버전 함정은 GPU 서버 쪽에만 있다 |
| 앱 | **Flutter (Android only)** | 크로스플랫폼을 포기했으므로 원래 명분은 사라졌다. 그럼에도 유지하는 이유는 ① `CustomPainter` 캔버스와 펫 애니메이션 구현이 빠르고 ② 팀이 이미 정한 스택이며 ③ **홈 위젯은 어느 쪽을 골라도 네이티브 Kotlin AppWidget을 직접 짜야 하므로**(`home_widget` 플러그인) Kotlin 전환의 이득이 작기 때문이다. |
| API | Python FastAPI | 비동기, GPU 서버 연동 용이 |
| 실시간 | Socket.IO (`python-socketio`) | Tunnel에서 유일하게 안전한 양방향 채널 |
| DB | MySQL 8 | |
| 미디어 | KCLOUD VM 파일시스템 | 7절 이슈 ② |
| **LLM** | 같은 서버 GPU에 **소형 한국어 LLM 상주 서빙** (OpenAI 호환 엔드포인트) | **예산 0원.** 6.2절 |
| 이미지 생성 | 같은 서버 GPU — **SD 1.5** + 기본 프리셋 / LoRA | 6.3절. SDXL은 20GB에서 학습이 어렵다 |
| 푸시 | FCM | Android 단일이므로 APNs 불필요 |

---

## 6. 상세 설계

### 6.1 실시간 계층 (Socket.IO) — 주 옵션

네임스페이스 `/rt`, 룸 키 `group:{group_id}`. 연결 시 토큰으로 인증하고 자신의 그룹 룸에 조인한다.

| 방향 | 이벤트 | 페이로드 | 대응 기능 |
|---|---|---|---|
| S→C | `doodle:new` | `{doodle_id, sender_id, mode, content_type}` — `thumb_url`은 `mode=normal`일 때만 | SD-5, SD-6 |
| S→C | `doodle:expired` | `{doodle_id}` | SD-6 |
| S→C | `poke` | `{from_user_id, at}` | SD-7 |
| S→C | `pet:levelup` | `{pet_id, level}` | PT-1 |
| S→C | `pet:activity` | `{pet_id, activity, utterance}` | PT-1 |
| C→S | `doodle:viewed` | `{doodle_id}` | SD-6 |
| C→S | `poke:send` | `{to_user_id}` | SD-7 |

**사라지기 모드 흐름.** 수신자가 뷰어에서 낙서를 열면 `doodle:viewed`를 보낸다. 서버는 `doodle_receipts`에 최초 확인 시각을 기록하고(`UNIQUE` 제약이 중복 확인을 막는다) `expires_at = now + 5s`를 세팅한 뒤, 5초 후 `deleted_at`을 채우고 미디어 파일을 실제로 지운 다음 `doodle:expired`를 룸에 브로드캐스트한다.

> **이 5초를 스케줄러 폴링으로 구현하면 안 된다.** 1초마다 DB를 스캔하면 최악 5초 지연에 매초 부하가 얹힌다. 확인 시점에 `loop.call_later(5, ...)`로 지연 태스크 하나를 걸고, `doodle_id → TimerHandle`을 들고 있다가 조기 삭제 시 취소한다. 타이머는 프로세스 인메모리이므로 **서버 재기동 시 부팅 스윕**으로 보강한다 — `expires_at`이 지난 건 즉시 정리하고, 아직 안 지난 건 남은 시간으로 재스케줄한다. 이 때문에 uvicorn은 **단일 프로세스**로 돌린다. 구현 상세는 [STACK.md](STACK.md) 6절.

**소켓과 푸시의 역할 분담.** 앱이 켜져 있으면 Socket.IO가, 백그라운드면 FCM 푸시(SD-7, SD-8, MR-1)가 알린다. 위젯 갱신은 FCM data 메시지로 트리거한다.

### 6.2 펫의 하루 — LLM 계층 (부 옵션)

**모델은 자체 GPU 서버에서 서빙한다. 외부 유료 API를 쓰지 않는다.** vLLM으로 OpenAI 호환 엔드포인트를 띄우고 FastAPI가 내부 HTTP로 호출한다.

**펫 활동 생성.** 스케줄러가 하루에 몇 차례 펫의 다음 활동을 만든다. 입력은 그룹의 최근 활동(전송 횟수, 마지막 낙서 유형, 찌르기 수)과 펫 레벨이고, 출력은 활동 종류와 그때 건네는 대사다.

```
입력: 레벨 3, 최근 24h 낙서 2건(그림 위주), 찌르기 1회
출력: { activity: "napping", utterance: "방금 밥 먹고 졸려… 그림 잘 봤어!" }
```

- 응답 형식을 강제해야 하므로 **구조화 출력(JSON 스키마)** 을 쓴다.
- `activity`는 열거값(`eating`, `sleeping`, `walking`, `playing`, `drawing`, `waiting`)으로 제한한다. 자유 문자열을 허용하면 그림 생성 프롬프트를 만들 수 없다.
- **쓰다듬기(PT-1)는 LLM을 부르지 않는다.** 현재 활동 행의 `utterance`를 그대로 돌려준다. 사용자가 연타할 수 있는 인터랙션이라 매 탭마다 추론을 돌리면 GPU가 버티지 못한다.

**그림 일기 생성.** 자정에 그날의 활동들을 모아 그림 1장 + 캡션 1문장을 만든다.

1. 그날의 `pet_activities`를 요약해 LLM이 캡션을 쓴다 — *"오늘은 낮잠을 오래 잤다. 둘이 보내준 그림이 좋았다."*
2. 활동 종류와 캡션으로 SD 프롬프트를 조립해 그림 1장을 생성한다.
3. 어떤 그림체(`style_model_id`)로 그렸는지 함께 기록한다.

### 6.3 그림체 — 기본 프리셋에서 시작해 사용자 그림체로

**기본 그림체(P1).** 그룹을 만들 때 `style_models`에 `kind='default'`, `version=0`, `status='ready'` 행을 미리 넣는다. 가입 첫날부터 일기가 그려진다.

**사용자 그림체(P2).** 그룹의 손그림 낙서(`content_type='drawing'`)가 **20장 이상** 쌓이면 학습 잡을 큐에 넣는다. GPU 서버에서 SD LoRA 파인튜닝을 돌리고 가중치 경로를 `style_models`(`kind='learned'`, `version=1`)에 기록한다. `status='ready'`가 되는 순간부터 **그 이후의 일기만** 새 그림체로 그린다. 과거 일기는 다시 그리지 않는다.

`pet_diaries.style_model_id`가 각 일기의 화풍을 기록하므로, 일기장을 넘기면 **어느 날부터 그림체가 바뀌었는지**가 그대로 보인다. 이것이 "펫이 우리 그림체를 배웠다"를 화면으로 증명하는 장치다.

> **왜 기본 그림체가 필요한가.** ① 가입 첫날은 학습할 낙서가 0장이다. ② LoRA에 쓸 만한 손그림은 현실적으로 20~30장이 필요한데 7일 안에 그만큼 쌓이지 않는다. ③ 기본 → 학습 전환이 펫 성장 서사와 정확히 맞아떨어진다.

> **리스크와 폴백.** LoRA 학습은 이 프로젝트에서 가장 먼저 무너질 지점이다. 폴백 우선순위 — ① img2img만 사용해 학습 단계 생략, ② 프리셋 몇 종만 두고 레벨에 따라 교체, ③ PT-5·PT-6b를 통째로 잘라낸다. 셋 중 무엇을 택해도 **PT-1(펫 대사)과 PT-6a(기본 그림체 일기)가 남으므로 LLM Wrapper 요건에는 영향이 없다.**

### 6.4 GPU 자원 경합

3090 한 대가 LLM 서빙과 SD를 겸한다. **VRAM은 24GB가 아니라 20GB다.** 상세 예산표와 근거는 [STACK.md](STACK.md) 2절에 있다. 여기서는 결론만 옮긴다.

| 워크로드 | 성격 | VRAM |
|---|---|---|
| LLM 서빙 — EXAONE 3.5 7.8B AWQ, `--gpu-memory-utilization 0.4` | **상주** | **8GB (상한 고정)** |
| SD 1.5 추론 (일기 그림 1장) | 자정 배치 | 4~6GB |
| SD 1.5 LoRA 학습 (512px, batch 1, 8-bit Adam) | 새벽 배치 (P2) | 6~8GB |
| Qwen3-VL 4B AWQ (최고의 낙서) | 월말 배치 (P2, 미도입) | 6~9GB |

- **상주 LLM + SD 추론** = 12~14GB. 자정 일기 배치는 LLM을 내리지 않아도 된다.
- **상주 LLM + LoRA 학습** = 14~16GB. 헤드룸이 4~6GB 남아 **동시 실행이 가능하다.** 다만 안전하게 가려면 학습 동안 LLM을 sleep 시킨다.
- **셋 동시(LLM + SD + VL)는 20~23GB로 불가능하다.** 반드시 시간대를 가른다.
- **SDXL은 쓰지 않는다.** 단독 학습은 20GB에 들어가지만(피크 13~15GB) 상주 LLM과는 21~23GB로 공존하지 못한다. SD 1.5로 간다.

**교대는 프로세스를 죽여서 하지 않는다.** vLLM을 `--enable-sleep-mode`로 띄우면 `POST /sleep`으로 가중치를 CPU RAM에 오프로드하고 GPU를 비웠다가, `POST /wake_up`으로 3~6초 만에 되살릴 수 있다. `--gpu-memory-utilization 0.4`가 상주 상한을 8GB로 못박고, sleep이 그 8GB마저 비운다. 이 둘이 20GB 공존의 축이다.

> **이전 버전의 수치 두 개가 틀렸다.** SD 1.5 LoRA 학습을 8~12GB로, Qwen3-VL 4B를 6~8GB로 적었는데, 앞의 것은 SDXL 수치였고 뒤의 것은 AWQ가 비전 인코더를 고정밀로 유지한다는 사실을 놓쳤다. 정정 내역은 [STACK.md](STACK.md) 8절.

### 6.5 이번 달 최고의 낙서 (MR-3)

**당분간 규칙 기반으로 정한다.**

1. 그 달의 낙서 중 **답장(`parent_id`)이 가장 많이 달린 낙서**
2. 동률이면 `stroke_data`의 획 수가 많은 낙서
3. 그래도 동률이면 최신 낙서

사라지기 모드 낙서는 삭제되므로 후보에서 제외된다. 어떤 규칙으로 골랐는지는 `monthly_reports.best_doodle_rule`에 남기므로, 나중에 선정 방식을 바꿔도 과거 레포트와 구분된다.

> **vision은 실제로 가능하다. 다만 지금은 쓰지 않기로 했다.**
>
> "3090이 감당 못 한다"는 초기 판단은 **멀티모달 모델을 상주시킨다고 전제했을 때만** 참이었다. MR-3는 월 1회·그룹당 1회 배치이므로 그 전제가 성립하지 않는다. 조사 결과 Qwen3-VL 4B(Apache 2.0, 2025년 10월 공개)를 4-bit 양자화하면 권장 VRAM이 6~8GB다. 상주 LLM(5~8GB 추정)과 합치면 11~16GB로, **20GB 안에 들어가긴 하지만 24GB 때만큼 여유롭지는 않다.** 월 1회 배치이므로 여의치 않으면 그 몇 분간 LLM을 내리면 그만이다. 도입 방법과 설계 제약은 [BACKLOG.md](BACKLOG.md) C절에 정리했다.
>
> 팀 결정에 따라 당분간 규칙 기반을 유지한다. 다만 그 규칙의 신호가 생각보다 빈약하다 — 7절 ⑤ 참조.

### 6.6 인증과 "로그인 나중에 추가"

**결론: 지금 설계만 갈라두면 어렵지 않다. 나중에 붙이는 건 테이블 한 개와 라우트 두 개 분량이다.**

어려운 건 로그인 기능 자체가 아니라 **이미 쓰고 있던 익명 유저를 계정에 잇는 일(account linking)** 이다. 익명 토큰을 `users`에 직접 박아두면 "이 기기의 그 사람"과 "카카오로 로그인한 이 사람"을 같은 유저로 묶을 방법이 사라진다. 그래서 처음부터 **사람(`users`)과 인증 수단(`auth_identities`)을 분리**한다.

```
users(id, display_name)                    ← "사람". 절대 안 바뀜
auth_identities(user_id, provider, provider_uid, secret_hash)
   provider = 'device'  ← 지금. 설치 시 발급한 익명 토큰
   provider = 'kakao'   ← 나중에 행 하나 추가하면 끝
```

- **지금:** `POST /auth/register`가 `users` 1행 + `auth_identities(provider='device')` 1행을 만들고 토큰을 돌려준다. 로그인 화면 없음.
- **나중에:** 로그인된 유저가 카카오를 연동하면 **같은 `user_id`에 `provider='kakao'` 행을 하나 더 붙인다.** 기존 그룹·낙서·펫이 그대로 따라온다.
- **감수하는 것:** 소셜 로그인을 붙이기 전까지 기기 변경·앱 삭제 시 계정 복구가 불가능하다.

---

## 7. 미해결 사항

① **2인 정원을 어디서 강제할 것인가.** MySQL은 "자식 행 2개 이하"를 선언적으로 막지 못한다. 애플리케이션 레벨 검사 + `group_members`에 `BEFORE INSERT` 트리거를 둔다. 동시 가입 경합은 트랜잭션에서 `SELECT ... FOR UPDATE`로 막는다.

② **"Object Storage: Kcloud VM"은 성립하지 않는다.** VM은 오브젝트 스토리지가 아니다. (a) VM 파일시스템 + FastAPI 정적 서빙, (b) VM에 MinIO를 올려 S3 호환 API 사용 중 일정을 감안해 **(a)** 로 가정했다.

③ **LLM 모델 선정이 아직 열려 있다.** 3090 **20GB**에 4-bit 양자화로 올릴 7~8B급 한국어 모델을 골라야 한다. 후보를 벤치마크한 뒤 정한다. 캠프에서 API 크레딧을 지원받을 수 있다면 외부 LLM API로 되돌리는 편이 품질·구현 난이도 모두 유리하다.

**GPU 서버가 Ubuntu 20.04라는 점이 변수다.** 기본 Python이 3.8이라 최신 추론 스택이 그대로 깔리지 않는다. 앱 VM은 Ubuntu 24.04(Python 3.12)라 이 문제가 없다. 환경 구성 경로는 별도 조사 중이며 결과가 나오면 반영한다.

④ **LoRA 학습 임계값 20장은 근거가 약하다.** 실제로 몇 장부터 그림체가 잡히는지 시험해 보고 조정한다.

⑤ **규칙 기반 MR-3의 신호가 빈약하다.** 2인 그룹에서 현재 스키마가 주는 **견고한 행동 신호는 답장 수 하나뿐이다.** `doodle_receipts`는 `UNIQUE(doodle_id, user_id)`로 최초 확인만 기록하므로 체류 시간도 재조회 횟수도 없고, 낙서에 대한 하트·북마크 테이블도 없다(`pet_likes`는 남의 그룹 펫용이다). 커플 2인·월 수십 장이면 답장 수는 동률이 잦고, 그러면 사실상 획 수로 결정되는데 **획 수는 정성이 아니라 활동량**이며 왕복 낙서로 부풀릴 수 있다.

→ **제안: `stroke_data`에 획별 타임스탬프를 넣자.** 그리기 소요 시간이라는 훨씬 나은 대리 지표가 생긴다. 캔버스가 이미 좌표를 찍고 있으므로 추가 비용이 사실상 없다. ERD 2절 참조. 다만 이것도 조작 가능한 활동량 지표라는 한계는 남는다.

⑥ **7일 일정 대비 기능 과다.** 36행 중 P2가 11개다. 펫 스토어·집 꾸미기·다른 그룹 탐색(EX-1~3)은 데모 임팩트가 작아 가장 먼저 잘라낼 후보다. 잘라낼 순서는 [BACKLOG.md](BACKLOG.md) D절.

⑦ **화면 설계는 별도 트랙.** Figma 화면 기획서는 다른 쪽에서 확정 후 반영하므로 이 문서는 화면 레이아웃을 규정하지 않는다. 2절의 진입 경로만 확정된 상태다.

⑧ **앱 VM과 GPU 서버가 서로 보이는지 확인되지 않았다. 지금 가장 큰 미확인 항목이다.**
   - 기본 가정: 두 인스턴스가 같은 KCLOUD 사설망에 있어 **앱 VM이 GPU 서버의 사설 IP로 직접 HTTP 호출**한다. 가장 단순하다.
   - 안 될 경우의 폴백: GPU 서버가 앱 VM의 **잡 큐를 폴링**한다(아웃바운드 전용). 앱 VM은 `pending` 잡을 큐에 넣고, GPU 서버가 가져가서 처리한 뒤 결과를 POST로 되돌린다. 방화벽에 구멍을 뚫지 않아도 되지만 구현이 하루 더 든다.
   - **Day 1에 `curl`로 확인하고 갈라져야 한다.** 이걸 미루면 Day 5에 발목을 잡는다.

⑨ **앱 VM의 RAM이 4GB뿐이다.** MySQL 8 + FastAPI + Socket.IO + 미디어 서빙을 4GB에 얹는다. 사용자가 둘뿐이라 트래픽은 문제가 아니지만, ① `innodb_buffer_pool_size`를 기본값 근처로 두고 ② 이미지 리사이즈·썸네일 생성 같은 메모리 튀는 작업을 앱 VM에서 하지 않는다. 무거운 이미지 처리는 GPU 서버로 보낸다.
