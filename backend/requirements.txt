# Memory Pager — 앱 VM 백엔드 (Ubuntu 24.04, Python 3.12)
#
# 24.04 는 PEP 668 로 시스템 pip 설치를 막는다. 반드시 venv 안에서 설치할 것.
#   python3 -m venv ~/envs/api && source ~/envs/api/bin/activate
#   pip install -r backend/requirements.txt
#
# 버전 근거와 검증 등급은 docs/STACK.md 3절 참조.
# GPU 서버(Ubuntu 20.04)의 의존성은 gpu/ 아래에 따로 있다. 이 파일과 섞지 말 것.

fastapi==0.139.0             # 2026-07-01 최신 안정. requires Python>=3.10
uvicorn[standard]==0.51.0    # [standard] 가 아니면 websockets 구현이 빠져 Socket.IO WS 업그레이드가 실패한다
python-socketio==5.16.3      # Socket.IO v5 / Engine.IO v4. Flutter socket_io_client 3.x 와 정합
sqlalchemy[asyncio]==2.0.51  # 2.1 은 아직 beta 라 2.0.x 고정. requires Python>=3.10
asyncmy==0.2.11              # MySQL async 드라이버. cp39~cp313 prebuilt wheel 이라 컴파일 불필요
apscheduler==3.11.3          # AsyncIOScheduler. 4.x 는 alpha
firebase-admin==7.5.0        # FCM data-only 메시지 (위젯 갱신, 찌르기, 낙서 수신, 월간 알람)
httpx==0.28.1                # GPU 서버 호출 (LLM / SD). 로컬에서 설치·동작 확인함
pydantic-settings==2.14.2    # .env 로드. 로컬에서 설치·동작 확인함

# --- 아래 둘은 아직 검증하지 않았다. 핀 없이 설치한 뒤 pip freeze 로 고정할 것. ---
python-multipart             # 낙서 전송이 multipart 업로드다
pillow                       # 썸네일 정도만. 무거운 이미지 처리는 GPU 서버로 넘긴다 (앱 VM RAM 4GB)
