-r requirements.txt

# 테스트 러너 — test_gpu_clients.py 등 pytest 스위트 실행에 필요.
# (문서의 `pytest tests/… -o asyncio_mode=auto` 를 깨끗한 환경에서 그대로 돌리려면 있어야 한다)
pytest==9.1.1
pytest-asyncio==1.4.0

# 실제 MySQL 통합 테스트의 동기 검증 쿼리
pymysql==1.2.0

# test_realtime_e2e.py의 python-socketio 비동기 클라이언트 transport
aiohttp==3.14.1

# 정적 검사와 신규 파일 포맷 확인
ruff==0.15.21
