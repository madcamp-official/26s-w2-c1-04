# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# 환경 변수 / 로컬 설정
.env
.env.*
!.env.example

# FCM 서비스 계정 키(비밀! 절대 커밋 금지). google-services.json 은 앱에 필요해 커밋한다.
*-firebase-adminsdk-*.json
**/service-account*.json
secrets/

# 미디어 · 모델 가중치 (VM 파일시스템에 둔다. 레포에 넣지 않는다)
media/
models/
*.safetensors
*.ckpt
*.bin
*.pt

# Flutter / Dart
app/build/
app/.dart_tool/
app/.flutter-plugins
app/.flutter-plugins-dependencies
app/android/local.properties
app/android/key.properties
*.jks

# IDE / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# 골든 실패 diff 아티팩트(테스트 실행 시 생성, 커밋 금지)
app/test/failures/
